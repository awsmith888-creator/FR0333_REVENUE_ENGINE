# FR0333 Adobe Mariah Carey CHOMP Reference Pack

`FR0333.ADOBE.MARIAH.CAREY.CHOMP.0001` is a child reference pack of `FR0333.AI.IMAGE.TOOL.METRICS.0001`.

It binds a dated Mariah Carey chronology and 60 atomic facts to the Adobe planning layer. It does not claim a new Golden Chain position, Adobe execution, ownership of a celebrity identity, or permission beyond the user's supplied image and requested transformation.

## Counts

- 51 statistical bits
- 9 money bits
- 60 total bits
- 9 chronological periods
- 7 explicit data holds

## Adobe upgrade

- Preserves `N_IN == N_OUT` for queues from one through ten inputs.
- Emits one independent `9:16` canvas per queue slot.
- Rejects collages, duplicate-output policy and input/output count mismatch.
- Requires an attached image when identity lock is requested.
- Treats ordinary clothed stage, performance and fashion imagery as standard celebrity-performance context.
- Preserves source wardrobe and pose unless the user requests a change.
- Keeps statistics out of rendered image text by default.
- Excludes unresolved `HOLD` facts from compiled safe context.
- Keeps contracts, buyouts, reported earnings and tour gross in separate money records; no net-worth aggregate is permitted.

## Evidence boundaries

`OBSERVED != CORRELATED != CAUSAL`

`REFERENCE.PACK != IDENTITY.AUTHORIZATION != ADOBE.RUNTIME.RECEIPT`

`LOCAL.SCHEMA.VALIDATION != EXTERNAL.ADOBE.EXECUTION`

Wikipedia remains a derivative discovery source. The Fresh Air Fund page is retained as an original organizational source for Camp Mariah, and the Associated Press report is retained as independent corroboration for the 2026 MusiCares recognition. Conflicting counts remain `HOLD`.

## Run

```bash
python3 -m unittest RavenCloudTaskbar.test_mariah_carey_chomp -v
```

The validation receipt establishes schema behavior only. It does not establish that Adobe generated, edited, stored or published an image.
